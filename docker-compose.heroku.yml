version: "3"
services:
  gateway:
    build:
      context: .   # is it necessary?
      dockerfile: gateway/Dockerfile
    image: gateway
    ports:
      - 8080:8080
  loyalty:
    build:
      context: .
      dockerfile: loyalty/Dockerfile
    image: loyalty
    ports:
      - 8081:8081
